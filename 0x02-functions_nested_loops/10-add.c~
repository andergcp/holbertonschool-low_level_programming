#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int add(int a, int b)
{
        int result;
	result = a + b;
	_putchar((result / 10) + '0');
	_putchar((result % 10) + '0');
	_putchar('\n');
	return(void);
}
